<h1>All Posts</h1>

<% @posts.each do |post| %>
    <div class="card-body">
        <h2><%= post.title %></h2>
        <p><%= post.body %></p>
        <p><%= link_to "Read more", post_path(post), :class => 'btn btn-default' %></p>
    </div>
<% end %>
